# プレイパーク来場者カウンター 運営マニュアル

来場者カウンターアプリの基本仕様、導入設定、および毎日のデータ管理・集計手順に関するスタッフ用説明書です。管理者様へ共有・印刷してご使用ください。

---

## 🌐 アプリの基本情報

* **公開URL**:  
  👉 **`https://kawazusa.github.io/playpark-counter/`**  
  *(タブレットのブラウザに入力して開くアドレスです。一度開けば、次回以降オフラインでも利用可能です)*

* **管理者用ログインパスコード**:  
  🔑 **`1234`**  
  *(または、画面右下の設定「⚙️（歯車）」アイコンを **5秒間長押し** するとパスコードなしで入れます)*

---

## 📱 1. タブレットでの利用推奨設定（全画面モード）

タブレットで一般の来場者（子どもや保護者）に入力してもらう際は、誤操作でブラウザを閉じたり別のサイトへ行ってしまったりするのを防ぐため、**ホーム画面に追加（PWAインストール）**して使用してください。

### iPad（Safari）の場合:
1. Safariブラウザで上記の **公開URL** を開きます。
2. 画面右上（または下部）にある **「共有（上矢印）アイコン」** をタップします。
3. メニューから **「ホーム画面に追加」** を選択します。
4. ホーム画面に作成された「来場者カウンター」アイコンからアプリを起動します。（アドレスバーが消え、全画面で起動します）

### Androidタブレット（Chrome）の場合:
1. Chromeブラウザで上記の **公開URL** を開きます。
2. 画面右上にある **「メニュー（縦の3点）アイコン」** をタップします。
3. **「アプリをインストール」** または **「ホーム画面に追加」** をタップします。
4. ホーム画面に作成されたアイコンから起動します。

---

## 📶 2. インターネット環境とオフライン動作

本アプリは **オフライン第一（Offline-First PWA）** で設計されています。
* **公園での動作**: モバイル通信やWi-Fiなどのインターネット接続がない屋外プレイパークでも動作します。
* **仕組み**: 登録データはタブレット（端末）内の安全なローカルデータベース（IndexedDB）に一時保存されます。

---

## ⚠️ 3. 運営時の重要注意点（データ消失防止）

データはすべてタブレットのブラウザ内にのみ保存されます。そのため、以下の操作を行うと **保存されている来場者データが完全に消えてしまいます**。データ集計が終わるまでは絶対に避けてください。

1. **ブラウザ履歴やデータの消去禁止**:
   * ブラウザ設定の「キャッシュ消去」「閲覧履歴の消去」「Cookie/サイトデータのクリア」を行うとデータベースが空になります。
2. **プライベートブラウズ（シークレットモード）での利用禁止**:
   * シークレットモードで開いたタブは、タブやブラウザを閉じた瞬間にデータがすべて自動で消去されます。必ず通常のブラウザモードで起動してください。

---

## 📊 4. 複数端末間でのデータ共有と集計手順

本アプリは、個人情報保護およびオフライン動作のため、クラウドサーバーにデータを送信しません。
* **データの所在**: 入力したデータは **「そのタブレットの中だけ」** にあります。複数台のタブレットを設置した場合、お互いのデータは同期されません。
* **集計手順**:
  1. 一日の運営終了後、各タブレットで管理者画面に入ります。
  2. **「CSV出力（Excel用）」** ボタンをタップし、それぞれの端末からCSVファイルをダウンロードします。
  3. 各CSVファイルをパソコン等に集め、Excel等にコピー＆ペーストして1つのシートに合算して集計します。

---

## 🧹 5. 一日の終わりのデータ消去（リセット）

古いデータが残ったままだと、次回の開催日のデータと混ざってしまいます。
* **手順**: 
  1. 各端末からCSVファイルが正しく書き出された（保存された）ことを確認します。
  2. 管理者画面のヘッダー右上にある **「データ全消去」** ボタンをタップします。
  3. 2回の警告ダイアログが表示されるので、確認して消去を実行します。これで次回の開催日に向けてデータが空（リセット）になります。
